#include<stdio.h>
#include<windows.h>

int main(){
SetConsoleOutputCP(65001);
SetConsoleCP(65001);
float n1, n2;
int opcao;

printf("%s", n > 0 ? "Positivo" : (n < 0 ? "Negativo" : "Zero"));
    











    return 0;
}